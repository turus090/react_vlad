1.создаем проект npx create-react-app (называем папку)client ;
 2. устанавливаем sass (npm i sass);
3.удаляем Арр файлы в src - создаем App.jsx ; 
4. создаем папку Компонентс и папку разделов сайтов; 
5. npm i react-router-dom для роутинга;

запускаем терминал. Пишем ssh root@ip;
ls - просмотр файлов 
pwd - посмотреть путь 
cd -  переход в корень 
cd путь 
cd ../ - назад
mv (путь файла) (новый путь) - перемещение 
git pull
NPM RUN START - ЗАПУСКАЕМ проект/закрываем
npm run build
ls

путь проекто /var/www
ls
rm -r -для удаления файла
ls
cd -переходим в корень
пишем cd и название проекта
 mv build /var/www
 ls

cd /var/www

 mv build vlad
 ls
 